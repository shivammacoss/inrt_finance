NEXT_PUBLIC_API_URL=http://localhost:5001
# Frontend dev port 3033 (package.json). Change both if ports clash.
# Razorpay Checkout key is returned by POST /payment/create-order (same as RAZORPAY_KEY_ID on the API).
