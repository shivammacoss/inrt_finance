NEXT_PUBLIC_API_URL=http://localhost:5001
# Subdomain admin host (middleware sends admin.*/ → /admin/login). Local: admin.localhost:3033
# NEXT_PUBLIC_ADMIN_HOST=admin.inrt.finance
# Link from marketing site to user login (shown on admin login page)
# NEXT_PUBLIC_USER_APP_ORIGIN=https://inrt.finance
# Frontend dev port 3033 (package.json). Razorpay key comes from POST /payment/create-order.
